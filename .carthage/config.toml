# Generated by /carthage-annex. Safe to edit; re-running the skill will
# offer to diff before overwriting.
[carthage]
version = "1"                               # config schema version
base_image_tag = "v1"            # carthage-base major this project targets
annexed_with_cli = "1.1.1"     # informational; what CLI did the annex
service_name = "dev"
project_slug = "wizard-battles"
